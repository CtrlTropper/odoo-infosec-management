<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 1. FORM VIEW: Giao diện chi tiết -->
    <record id="view_infosec_asset_form" model="ir.ui.view">
        <field name="name">infosec.asset.form</field>
        <field name="model">infosec.asset</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="status" widget="statusbar" statusbar_visible="active,maintenance,compromised"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Ví dụ: FW-DC-PaloAlto-01"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="ip_address"/>
                            <field name="device_type"/>
                        </group>
                        <group>
                            <field name="status"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- 2. LIST VIEW (Thay thế cho Tree View cũ) -->
    <record id="view_infosec_asset_list" model="ir.ui.view">
        <field name="name">infosec.asset.list</field>
        <field name="model">infosec.asset</field>
        <field name="arch" type="xml">
            <list decoration-danger="status == 'compromised'" decoration-info="status == 'maintenance'">
                <field name="name"/>
                <field name="ip_address"/>
                <field name="device_type"/>
                <field name="status" widget="badge" 
                       decoration-success="status == 'active'" 
                       decoration-warning="status == 'maintenance'" 
                       decoration-danger="status == 'compromised'"/>
            </list>
        </field>
    </record>

    <!-- 3. ACTION -->
    <record id="action_infosec_asset" model="ir.actions.act_window">
        <field name="name">Thiết bị Mạng</field>
        <field name="res_model">infosec.asset</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- 4. MENUS -->
    <menuitem id="menu_infosec_root" name="InfoSec" sequence="10"/>
    <menuitem id="menu_infosec_asset" name="Quản lý Thiết bị" parent="menu_infosec_root" action="action_infosec_asset" sequence="10"/>
    
    <!-- 5. SERVER ACTION: Nút Cách ly khẩn cấp -->
    <record id="action_quarantine_assets" model="ir.actions.server">
        <field name="name">Cách ly Khẩn cấp (Quarantine)</field>
        <field name="model_id" ref="model_infosec_asset"/>
        <field name="binding_model_id" ref="model_infosec_asset"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            # Đoạn code Python này sẽ lặp qua các bản ghi được tick chọn và đổi trạng thái
            for record in records:
                record.write({'status': 'compromised'})
        </field>
    </record>
</odoo>